interface:
  display_name: "Mklink AI Probe"
  short_description: "Flash, RTT/VOFA, memory dump/write, serial, and Modbus debugging."
  default_prompt: "Use $mklink-ai-probe to initialize an embedded project, flash firmware, inspect RTT/VOFA data, dump or write memory, manage serial resources, or build a Modbus point-table dashboard."

policy:
  allow_implicit_invocation: true
